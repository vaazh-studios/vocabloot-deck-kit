// Image provider "none": the creator makes the images with any tool and drops
// stickers/source/<slug>.png into the folder; the kit only checks and prepares.
export async function generate() {
  return null;
}
